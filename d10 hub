local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "d10 Hub",
   Icon = 0,
   LoadingTitle = "Carregando...",
   LoadingSubtitle = "by d10",
   Theme = "Default",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "d10Data", 
      FileName = "d10 Hub"
   },
   KeySystem = false
})

local maintab = Window:CreateTab("Main", 4483362458)

-- Notificação inicial
Rayfield:Notify({
   Title = "d10 Hub Ativado",
   Content = "Bem-vindo, d10!",
   Duration = 6.5,
   Image = 4483362458,
})

-- Variáveis de controle do Loop
local running = false
local thread = nil

-- Referência do Remote (Protegida com pcall caso o caminho mude)
local success, remote = pcall(function()
    return game:GetService("ReplicatedStorage")
        :WaitForChild("Packages")
        :WaitForChild("_Index")
        :WaitForChild("sleitnick_knit@1.7.0")
        :WaitForChild("knit")
        :WaitForChild("Services")
        :WaitForChild("SeasonService")
        :WaitForChild("RF")
        :WaitForChild("RequestRankedReward")
end)

-- Toggle corrigido
local tokenToggle = maintab:CreateToggle({
   Name = "Auto Token",
   CurrentValue = false,
   Flag = "TokenFlag", 
   Callback = function(Value)
      running = Value

      if running then
         -- Inicia o loop em uma thread separada
         if not thread then
            thread = task.spawn(function()
               while running do
                  if success and remote then
                     pcall(function()
                        remote:InvokeServer(1)
                     end)
                  else
                     warn("Remote não encontrado!")
                  end
                  task.wait(5) -- Espera 5 segundos entre as requisições
               end
               thread = nil
            end)
         end
      else
         running = false
         thread = nil
      end
   end,
})
